# Screenshots

Drop screenshots here for the main README. Expected filenames:

| File | Shows |
| --- | --- |
| `hero.png` | Full main window (video + timeline + export) — used as the header image |
| `timeline.png` | Aligning the recording to the match schedule on the Event Timeline |
| `export.png` | Batch export with per-match status and live progress |
| `settings.png` | Clip settings (padding, match length, dead air) in Preferences |
| `event.png` | Importing/editing an event from The Blue Alliance |

Suggested width ~1600px (retina). PNG preferred.
